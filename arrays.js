let kids = ['ben', 'jen', 'polly'];
let nums = [1,2,3,4]


// accessing info from array
kids[0];    // ben
num[3]; // 4


// changing info in an array
kids[0] = 'billy';
// because nothing in index 3 the value is added
kids[3] = 'cat'
kids;   //  'ben', 'jen', 'polly', 'cat'

// accessing the last item
kids[kids.length - 1]
// adding in the last item
kids[kids.length]= 'zan';
